# Local default (docker compose)
DATABASE_URL=postgresql+psycopg://rag:rag@postgres:5432/rag

# Optional: enable real OpenAI calls
# OPENAI_API_KEY=...
OPENAI_MODEL=gpt-5
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

CHUNK_SIZE=800
CHUNK_OVERLAP=120
